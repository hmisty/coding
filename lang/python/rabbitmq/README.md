install rabbitmq-server @CentOS:

```
$ sudo yum install rabbitmq-server
$ sudo service rabbitmq-server start
```

start consumer:

```
$ python consumer1.py
```

start producer:

```
$ python producer1.py
```
